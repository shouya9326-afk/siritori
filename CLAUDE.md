# CLAUDE.md（しりとりアプリ）

言語・共通ルールは親の `CLAUDE.md`（`../CLAUDE.md`）を参照。

## プロジェクト概要

jig.jp サマーインターンシップ（SABERAコース）選考課題。
ターミナル上で動作する「しりとりアプリ」を C言語 で実装する。
提出物は、ソースコード一式 + README.md を載せた **公開（Public）GitHubリポジトリ** のリンク。

## 作業ルール（Claudeへの指示・最優先）

学習を兼ねたプロジェクトのため、以下を厳守する。

### 1. Git操作は本人が手動で行う

- `git add`（ステージング）・`git commit`・`git push` などのGit操作は、
  **学習のためユーザー本人が手動で実行する**。Claudeは代行しない。
- Claudeの役割は、必要なコマンドと手順を **説明する** ところまで。実行はしない。

### 2. 何かを追加するときは事前に確認を取る

ファイル・フォルダ・パッケージ・設定ファイル（json等）を新規に追加する際は、
**追加する前に以下の点を説明し、確認を得てから進める**。

1. **なぜ** 追加するのか（理由・目的）
2. **何か**（そのものの役割）
3. **どうやって**（中身の生成方法）
4. 追加すると **何が起きるか**（影響）
5. **全体の中での位置づけ**（どういう要素か）

※ ユーザーが「もう確認不要」と明示するまで継続する。これにより、ユーザーが再現性のある技術を身につけることを目指す。

## ビルド・実行

```
gcc shiritori.c -o shiritori && ./shiritori
```

- Windows環境では、課題の案内どおり **WSL（Ubuntu）上の gcc** を使う想定。
- ソースを変更したら再コンパイルが必要。

## 必須仕様（提出時に満たすこと）

- 直前の単語を表示できる
- 任意の単語を入力できる
- 直前の単語の末尾と入力単語の先頭が一致する場合だけ更新、違えばエラー表示
- 末尾が「ん」で終わる単語が入力されたらゲーム終了
- 過去に使用した単語が入力されたらゲーム終了
- ゲーム中・終了後に最初からやり直せるリセット機能
- 加えて、便利・面白い任意の機能を **2つ以上** 実装

### UTF-8メモ

- ひらがな1文字 = 3バイト。`strlen` はバイト数を返す（「りんご」は 9）。
- 「ん」= `0xE3 0x82 0x93`。
- ひらがなの範囲 = `0xE3 0x81 0x80` ～ `0xE3 0x82 0x9F`。

## README.md に必須の項目

- ビルド方法・実行方法
- 動作の様子が分かるスクリーンショット
- 実装した機能の詳細な説明
- 参考にしたWebサイト
- AIの活用方法（どの部分にどう使ったか）
